<div class="fading-suns chat-card item-card">
  <header class="card-header">
    <img src="{{item.img}}" alt="{{item.name}}"/>
    <h3>{{item.name}}</h3>
  </header>
  <div class="card-body">{{{description}}}</div>
</div>
