{
  "name": "fading-suns",
  "version": "0.19.0",
  "private": true,
  "type": "module",
  "description": "Build tooling for the Fading Suns Foundry VTT system.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "validate": "node tools/validate.mjs",
    "build:packs": "node tools/build-packs.mjs",
    "build": "npm run build:packs && npm run validate && npm test"
  },
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^1.0.4",
    "classic-level": "^1.4.1",
    "handlebars": "^4.7.8"
  }
}
